name: (▶) Maven Release

on: workflow_dispatch

jobs:
  release:
    uses: sitepark-com/project-workflow/.github/workflows/maven-release.yml@main
    with:
      botName: "sitepark-bot"
      botEmail: "github@sitepark.com"
    secrets:
      SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      GPG_KEY_ID: ${{ secrets.GPG_KEY_ID }}
      GPG_KEY: ${{ secrets.GPG_KEY }}
      MVN_REPO_SERVERS: ${{ secrets.MVN_REPO_SERVERS }}
      MVN_REPO_SERVER_ID: ${{ secrets.MVN_REPO_SERVER_ID }}
      MVN_REPO_SERVER_URL: ${{ secrets.MVN_REPO_SERVER_URL }}

