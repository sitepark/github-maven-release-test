name: (📡) Verify

on:
  push:
    branches-ignore:
    - 'main'
    - 'dependabot/**'
  pull_request:
    branches-ignore:
    - 'main'
  workflow_dispatch:

jobs:
  verify:
    uses: sitepark/github-project-workflow/.github/workflows/actions/type/maven/verify.yml@feat/factor-structure

