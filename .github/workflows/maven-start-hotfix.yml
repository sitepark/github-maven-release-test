name: Start Maven Hotfix

on: workflow_dispatch

jobs:
  start-hotfix:
    uses: sitepark-com/project-workflow/.github/workflows/maven-start-hotfix.yml@main
    with:
      version: ${{ github.ref }}

